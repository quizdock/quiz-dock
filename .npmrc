auto-install-peers=true
strict-peer-dependencies=false
# Ne pas réinstaller/purger automatiquement les deps avant chaque `pnpm run`
# (sinon échec sans TTY dans les conteneurs de dev — pnpm 11).
verify-deps-before-run=false
confirm-modules-purge=false
